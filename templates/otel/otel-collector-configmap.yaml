apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-config
  namespace: {{ $.Values.namespace.name }}
data:
  config.yaml: |
    {{- tpl (.Files.Get "configs/otel-collector.yaml") . | trimSuffix "\n" | nindent 4 }}
