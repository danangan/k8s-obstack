apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-config
  namespace: {{ $.Values.namespace.name }}
data:
  loki.yaml: |
    {{- tpl (.Files.Get "configs/loki.yaml") . | trimSuffix "\n" | nindent 4 }}
